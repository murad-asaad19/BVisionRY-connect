/// Re-export of the [opportunitiesServiceProvider] so feature code can
/// `import` the providers package without dragging in the data layer.
library;

export '../data/opportunities_service.dart' show opportunitiesServiceProvider;
